# Puppet CI Resources

classes:
  - nsidc_jenkins
  - nsidc_nfs

# NFS Mounts
nsidc_nfs::sharemounts:
  /share/sw/packages:
    project: sw
    share: packages

# Jenkins Plugins
nsidc_jenkins::plugins:
  git: {}
  git-client: {}
  git-parameter: {}
  scm-api: {}
  credentials: {}
  ssh-credentials: {}
  greenballs: {}
  jobConfigHistory: {}
  mailer: {}
  instant-messaging: {}
  jabber: {}

# Jenkins Jobs
nsidc_jenkins::jobs:
  # clone the project into the shared workspace
  "%{hiera('project')}_1_Checkout_Project":
    gitrepo: git@bitbucket.org:nsidc/dataset-search-services.git
    workspace: /var/lib/jenkins/workspace
    trigger_job: "%{hiera('project')}_2_Configure_System"

  # use puppet to install system dependencies, such as Bundler
  "%{hiera('project')}_2_Configure_System":
    command: |
      cd puppet
      librarian-puppet clean
      librarian-puppet update
      cd -
      sudo puppet apply --environment=ci --debug --verbose --modulepath=./puppet/modules --hiera_config=./puppet/hiera.yaml ./puppet/site.pp

      # Because running `puppet apply` modifies the config.xml for each job
      # defined in ci.yaml, Jenkins has trouble triggering the next job through
      # normal methods (ie, using the trigger_job param), but we can use the
      # Jenkins API to trigger the next job with curl. Just make sure the name
      # of the job in the curl command is correct:
      sleep 15
      echo "Triggering a new build of %{hiera('project')}_3_Install_Dependencies"
      curl localhost:8080/job/%{hiera('project')}_3_Install_Dependencies
    workspace: /var/lib/jenkins/workspace

  "%{hiera('project')}_3_Install_Dependencies":
    command: bundle install
    workspace: /var/lib/jenkins/workspace
    trigger_job: "%{hiera('project')}_4_Check_Syntax"

  "%{hiera('project')}_4_Check_Syntax":
    command: bundle exec rubocop
    workspace: /var/lib/jenkins/workspace
    trigger_job: "%{hiera('project')}_5_Run_Unit_Tests"

  "%{hiera('project')}_5_Run_Unit_Tests":
    command: bundle exec rake spec:unit
    workspace: /var/lib/jenkins/workspace
    trigger_job: "%{hiera('project')}_6_Provision_Integration"

  "%{hiera('project')}_6_Provision_Integration":
    command: |
      ENV=integration

      (vagrant nsidc hijack --env=$ENV || true)
      vagrant nsidc up --env=$ENV
      vagrant nsidc ssh --env=$ENV -c 'sudo mkdir -p /opt/search_services'
      vagrant nsidc ssh --env=$ENV -c 'sudo cp -R /vagrant/* /opt/search_services'
      vagrant nsidc ssh --env=$ENV -c 'sudo chown -R vagrant /opt/search_services'
      bundle exec rake start_puma[$ENV]
    workspace: /var/lib/jenkins/workspace
    trigger_job: "%{hiera('project')}_7_Run_Integration_Tests"

  "%{hiera('project')}_7_Run_Integration_Tests":
    command: |
      export TARGET_ENVIRONMENT=integration
      bundle exec rake spec:acceptance
    workspace: /var/lib/jenkins/workspace

  "%{hiera('project')}_QA_Checkout_Project":
    parameters:
      - type: string
        name: tag
        description: git tag to checkout
        default: v1.0.0
    command: |
      (git clone git@bitbucket.org:nsidc/dataset-search-services.git . || true)
      git checkout $tag
    workspace: /var/lib/jenkins/workspace-qa
    trigger_job: "%{hiera('project')}_QA_Install_Dependencies"

  "%{hiera('project')}_QA_Install_Dependencies":
    command: bundle install
    workspace: /var/lib/jenkins/workspace-qa
    trigger_job: "%{hiera('project')}_QA_Check_Syntax"

  "%{hiera('project')}_QA_Check_Syntax":
    command: bundle exec rubocop
    workspace: /var/lib/jenkins/workspace-qa
    trigger_job: "%{hiera('project')}_QA_Run_Unit_Tests"

  "%{hiera('project')}_QA_Run_Unit_Tests":
    command: bundle exec rake spec:unit
    workspace: /var/lib/jenkins/workspace-qa
    trigger_job: "%{hiera('project')}_QA_Provision"

  "%{hiera('project')}_QA_Provision":
    command: |
      ENV=qa

      (vagrant nsidc hijack --env=$ENV || true)
      vagrant nsidc up --env=$ENV
      vagrant nsidc ssh --env=$ENV -c 'sudo mkdir -p /opt/search_services'
      vagrant nsidc ssh --env=$ENV -c 'sudo cp -R /vagrant/* /opt/search_services'
      vagrant nsidc ssh --env=$ENV -c 'sudo chown -R vagrant /opt/search_services'
      bundle exec rake start_puma[$ENV]
    workspace: /var/lib/jenkins/workspace-qa
    trigger_job: "%{hiera('project')}_QA_Run_Integration_Tests"

  "%{hiera('project')}_QA_Run_Integration_Tests":
    command: |
      export TARGET_ENVIRONMENT=qa
      bundle exec rake spec:acceptance
    workspace: /var/lib/jenkins/workspace-qa

  "%{hiera('project')}_Staging_Checkout_Project":
    parameters:
      - type: string
        name: tag
        description: git tag to checkout
        default: v1.0.0
    command: |
      (git clone git@bitbucket.org:nsidc/dataset-search-services.git . || true)
      git checkout $tag
    workspace: /var/lib/jenkins/workspace-staging
    trigger_job: "%{hiera('project')}_Staging_Install_Dependencies"

  "%{hiera('project')}_Staging_Install_Dependencies":
    command: bundle install
    workspace: /var/lib/jenkins/workspace-staging
    trigger_job: "%{hiera('project')}_Staging_Check_Syntax"

  "%{hiera('project')}_Staging_Check_Syntax":
    command: bundle exec rubocop
    workspace: /var/lib/jenkins/workspace-staging
    trigger_job: "%{hiera('project')}_Staging_Run_Unit_Tests"

  "%{hiera('project')}_Staging_Run_Unit_Tests":
    command: bundle exec rake spec:unit
    workspace: /var/lib/jenkins/workspace-staging
    trigger_job: "%{hiera('project')}_Staging_Provision"

  "%{hiera('project')}_Staging_Provision":
    command: |
      ENV=staging

      (vagrant nsidc hijack --env=$ENV || true)
      vagrant nsidc up --env=$ENV
      vagrant nsidc ssh --env=$ENV -c 'sudo mkdir -p /opt/search_services'
      vagrant nsidc ssh --env=$ENV -c 'sudo cp -R /vagrant/* /opt/search_services'
      vagrant nsidc ssh --env=$ENV -c 'sudo chown -R vagrant /opt/search_services'
      bundle exec rake start_puma[$ENV]
    workspace: /var/lib/jenkins/workspace-staging
    trigger_job: "%{hiera('project')}_Staging_Run_Integration_Tests"

  "%{hiera('project')}_Staging_Run_Integration_Tests":
    command: |
      export TARGET_ENVIRONMENT=staging
      bundle exec rake spec:acceptance
    workspace: /var/lib/jenkins/workspace-staging



  # If your project builds a package, these jobs provide a template for the
  # release pipeline for your package. Just replace the commented-out rake
  # commands with your own project's task runner.
  "%{hiera('project')}_Release_1_Bump_Version":
    choice_parameter:
      name: version_type
      choices:
      - patch
      - minor
      - major
    command: # echo $version_type
    workspace: /var/lib/jenkins/workspace
    trigger_job: "%{hiera('project')}_Release_2_Tag"

  "%{hiera('project')}_Release_2_Tag":
    command:
    workspace: /var/lib/jenkins/workspace
    trigger_job: "%{hiera('project')}_Release_3_Build_Release_Package"

  "%{hiera('project')}_Release_3_Build_Release_Package":
    command:
    workspace: /var/lib/jenkins/workspace
    trigger_job: "%{hiera('project')}_Release_4_Publish_Release_Package"

  "%{hiera('project')}_Release_4_Publish_Release_Package":
    command:
    workspace: /var/lib/jenkins/workspace
    trigger_job: "%{hiera('project')}_Release_5_Push_to_Git"

  "%{hiera('project')}_Release_5_Push_to_Git":
    command:
    workspace: /var/lib/jenkins/workspace
